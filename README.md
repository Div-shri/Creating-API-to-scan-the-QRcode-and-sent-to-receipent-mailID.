# Creating-API-to-scan-the-QRcode-and-sent-to-receipent-mailID.
I have made API where If I upload the file which is in CSV format then the csv contained data make QRcode and this QRcode contain the name, emailID, USeriD, URL and the user which received the email will scan the QRCODE and it should redirect to the URL which is mentioned in csv file.
